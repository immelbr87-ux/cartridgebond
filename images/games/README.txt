Place 28 Nintendo Switch box art images here.
See BOX_ART_MANIFEST.md in the repo root for the exact filenames needed.
Each file should be JPG, roughly portrait (3:4 ratio), ideally 600x800px or similar.
If an image is missing, the value page shows a clean text fallback instead of a broken image.
